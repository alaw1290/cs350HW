package hw3;

public class MM1Event {
	private double lambda;
	private double serviceRate;
	private double loggingTime;
	
	public MM1Event(double lamb, double sr, double tl){
		// Constructor: sets arrival, service, and logging rate
		lambda = lamb;
		serviceRate = sr;
		loggingTime = tl;
	}
	
	private double expDist(double lambda){
		// Input lambda (interarrival time) to return exponentially distributed values
		double result = -1*Math.log(1-Math.random()) / lambda;
		return result;
	}
	
	public double nextBirth(double currentTime){
		// Returns time of next birth event
		return currentTime + expDist(lambda);
	}
	

	public double nextDeath(double currentTime){
		// Returns time of next death event
		return currentTime + expDist(serviceRate);
	}

	public double nextLog(double currentTime){
		// Returns time of next log event
		return currentTime + expDist(loggingTime);
	}
	
}

public class EventObject extends MM1Event{
	
}


